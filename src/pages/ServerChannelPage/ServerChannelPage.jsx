import BaseLayout from 'layout/BaseLayout';

const ServerChannelPage = () => {
  return (
    <div id="server-channel">
      <BaseLayout
        navHeader="`2`"
        navArticle="`3`"
        navFooterWrapper="`4`"
        navFooterContainer="`5`"
        sectionHeader="`6`"
        sectionArticle="`7`"
        sectionAside="`8`"
      />
    </div>
  );
};

export default ServerChannelPage;
